package com.knowledgegraph.core.schema;

/** Allowed value types for a schema-defined entity/relationship property (contracts/openapi.yaml PropertyDefinition.dataType). */
public enum PropertyDataType {
    STRING,
    INTEGER,
    FLOAT,
    BOOLEAN,
    DATE
}
